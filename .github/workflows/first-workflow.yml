name: My first workflow
on: push

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:

    -name: Welcome Message
      run: echo "hello"

    -name: List of files
      run: ls

    -name: read the file
